#ifndef CREDENTIALS_H
#define CREDENTIALS_H

// WiFi Credentials
const char* ssid = "xxx";
const char* password = "xxx";

// MQTT Broker Details
const char* mqttServer = "yyyy"; // Replace with your MQTT broker
const int mqttPort = 1883;
const char* mqttUser = "xxx";       // If no username, leave blank
const char* mqttPassword = "xxx";   // If no password, leave blank

#endif // CREDENTIALS_H
